<%= form_with(model: brigade_membership, local: true) do |form| %>
  <% if brigade_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brigade_membership.errors.count, "error") %> prohibited this brigade_membership from being saved:</h2>

      <ul>
      <% brigade_membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :brigade_membership_student_id %>
  </div>

  <div class="field">
    <%= form.label :brigade_id %>
    <%= form.text_field :brigade_id, id: :brigade_membership_brigade_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
