<%= form_for([@brigade_membership.brigade, @brigade_membership]) do |form| %>
  <% if @brigade_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brigade_membership.errors.count, "error") %> prohibited this brigade_membership from being saved:</h2>

      <ul>
      <% @brigade_membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.select :student_id, Student.all.collect { |p| [ p.last_name+' '+p.first_name+' '+p.second_name, p.id ] } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
