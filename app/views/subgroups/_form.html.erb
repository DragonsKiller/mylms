<%= form_for([@subgroup.group, @subgroup]) do |f| %>
  <% if @subgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subgroup.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @subgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :subgroup_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
