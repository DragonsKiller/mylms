<%= form_for([@subgroup.group, @subgroup]) do |f| %>
  <% if @subgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subgroup.errors.count, "error") %> prohibited this subgroup from being saved:</h2>

      <ul>
      <% @subgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name%>
  </div>

  <div class="field">
    <%= f.label :group_id %>
    <%= f.text_field :group_id, :disabled=>true%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
