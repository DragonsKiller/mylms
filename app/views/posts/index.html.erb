<%= link_to('Logout teacher', destroy_teacher_session_path, method: :delete) %>
<%= link_to('Logout student', destroy_student_session_path, method: :delete) %>

<% @posts.each do |post| %>
     <h1><%= link_to "post.title", post %></h1>
     <p><%= post.content %></p>
<% end %>
