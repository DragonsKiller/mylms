<p id="notice"><%= notice %></p>

<h1>Campus</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @campus.each do |campus| %>
      <tr>
        <td><%= link_to 'Show', campus %></td>
        <td><%= link_to 'Edit', edit_campu_path(campus) %></td>
        <td><%= link_to 'Destroy', campus, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Campu', new_campu_path %>
